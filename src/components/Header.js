import React, { Fragment } from 'react'

const Header = () => (
        <Fragment>
                <header>
                        <h1>POKAPI</h1>
                </header>
        </Fragment>

);

export default Header;