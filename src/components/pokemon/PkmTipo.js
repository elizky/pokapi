import React from 'react'


const PkmTipo = ({ pokemon }) => {
    return (
        <div className="container-tipo">
            {/* ${tipos(pokemon)} */}
        </div>

    );
}

export default PkmTipo;